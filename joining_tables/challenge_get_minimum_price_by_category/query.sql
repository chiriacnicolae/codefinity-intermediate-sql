select category.name as category_name  , min(product.price)  as min_price  from category JOIN product on category.id = product.category_id group by category.name  HAVING COUNT(product.name) > 5
order by category_name;